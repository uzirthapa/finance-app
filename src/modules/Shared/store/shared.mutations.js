const mutations = {
    updateSharedTransactions(state, value) {
        state.sharedTransactions = value
    },
    updateSharedWithUsers(state, value) {
        state.sharedWithUsers = value
    },
    updateLoading(state, value) {
        state.loading, value
    }
}

export default mutations